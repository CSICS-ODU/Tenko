# Tenko — tested environment pins (Python 3.14.x in repo .venv).
# Optional full freeze: requirements.lock.txt (transitive-inclusive).
#
# Install:  python -m pip install -r requirements.txt

# --- Core numerics / data ---
numpy==2.5.1
scipy==1.18.0
pandas==3.0.5
scikit-learn==1.9.0

# --- Plotting ---
matplotlib==3.11.1

# --- Packet parsing / feature extraction ---
scapy==2.7.0

# --- Pipeline utilities ---
tqdm==4.70.0
psutil==7.2.2
requests==2.34.2

# --- Optional Cython fast path (fastpath/, setup_cython.py) ---
Cython==3.2.9

# Optional (not required for core Tenko + KitNET):
#   dpkt, torch, tensorflow, clingo
